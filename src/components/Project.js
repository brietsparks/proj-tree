import React from 'react';

const Project = ({
  title,
  subtitle,
  about,
  contributions,
  childProjects,

  showBody,
  showAggregates,
  showContributions,
  showProjects,
}) => {
  console.log({
    title,
    subtitle,
    about,
    contributions,
    childProjects,

    showBody,
    showAggregates,
    showContributions,
    showProjects,
  })
  return (
    <div>

    </div>
  );
};

export default Project;